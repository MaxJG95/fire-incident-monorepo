import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#d32f2f",
    },
    secondary: {
      main: "#0288d1",
    },
  },
  shape: {
    borderRadius: 12,
  },
});

export default theme;
